version: "3"

services:
  api-gateway:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    container_name: api-gateway
    volumes:
      - ./:/app
      - /app/node_modules
    ports:
      - 4000:4000